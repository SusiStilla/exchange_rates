name: exchange_rates
description: "Flutter app that displays exchange rates relative to a currency."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.1
  http: ^1.2.1
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  flutter_dotenv: ^5.1.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true